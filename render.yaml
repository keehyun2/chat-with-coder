services:
  - type: web
    name: chat-with-coder-backend
    runtime: node
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm start
    envVars:
      - key: PORT
        value: 3001
      - key: CLIENT_ORIGIN
        value: https://your-vercel-app.vercel.app
    healthCheckPath: /
